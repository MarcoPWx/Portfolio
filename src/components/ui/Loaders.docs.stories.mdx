import { Meta, Title, Subtitle, Description, ArgsTable, Source, Canvas, Controls } from '@storybook/blocks';
import { Progress, CircularProgress } from './Progress';
import * as LoadersStories from './Loaders.stories';
import { Skeleton as AdvancedSkeleton, SkeletonCard, SkeletonProjectCard, SkeletonSkillCard, SkeletonStatCard, SkeletonTable, SkeletonList, SkeletonProfile, SkeletonNavigation, SkeletonSection } from './Skeleton';

<Meta of={Progress} title="UI/Loaders/Docs" tags={['docs']} />

<Title>Loaders & Progress</Title>

<Subtitle>Linear and circular progress indicators, plus skeleton patterns</Subtitle>

<Description>
Use Progress for determinate loading percentages and CircularProgress for a compact radial indicator.
Skeleton components provide perceived performance while content loads.
</Description>

## Live examples

<Canvas of={LoadersStories.LinearProgress} />
<Source of={LoadersStories.LinearProgress} />
<Canvas of={LoadersStories.Circular} />
<Source of={LoadersStories.Circular} />
<Canvas of={LoadersStories.Skeletons} />
<Source of={LoadersStories.Skeletons} />

## Linear Progress

<Source
  code={`import { Progress } from '@/components/ui/Progress';

<div className="space-y-3">
  <Progress value={30} label="Uploading" showValue />
  <Progress value={75} variant="success" />
  <Progress value={50} variant="warning" size="lg" />
</div>`}
  language="tsx"
/>

<ArgsTable of={Progress} />

## Circular Progress

<Source
  code={`import { CircularProgress } from '@/components/ui/Progress';

<div className="flex items-center gap-6">
  <CircularProgress value={30} />
  <CircularProgress value={70} size={80} strokeWidth={6} variant="warning" />
</div>`}
  language="tsx"
/>

<ArgsTable of={CircularProgress} />

## Skeletons

<Source
  code={`import { Skeleton as AdvancedSkeleton, SkeletonCard, SkeletonProjectCard, SkeletonSkillCard } from '@/components/ui/Skeleton';

<div className="space-y-4">
  <AdvancedSkeleton variant="text" width="60%" />
  <SkeletonCard />
  <SkeletonProjectCard />
  <SkeletonSkillCard />
</div>`}
  language="tsx"
/>

- Additional skeletons: SkeletonStatCard, SkeletonTable, SkeletonList, SkeletonProfile, SkeletonNavigation, SkeletonSection

<Source
  code={`<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
  <SkeletonStatCard />
  <SkeletonTable rows={3} columns={4} />
  <SkeletonList items={3} />
  <SkeletonProfile />
</div>`}
  language="tsx"
/>

## Accessibility guidelines

- Progress bars should include a visible label or be accompanied by text context.
- For skeletons, ensure that focus does not land on placeholder elements.
- Prefer determinate indicators when the total progress is known.

