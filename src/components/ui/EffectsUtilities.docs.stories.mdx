import { Meta, Title, Subtitle, Description, ArgsTable, Source } from '@storybook/blocks';
import { AnimatedCounter, AnimatedPercentage, AnimatedStat } from './AnimatedCounter';
import { FloatingParticles } from './FloatingParticles';
import { ScrollProgress, BackToTop } from './ScrollProgress';

<Meta title="UI/Effects & Utilities/Docs" tags={['docs']} />

<Title>Effects & Utilities</Title>

<Subtitle>Animated counters, background particles, and scroll utilities</Subtitle>

<Description>
Use these utilities to add subtle motion and helpful navigation hints: animated counters for KPIs,
background particle canvas for ambient motion, and scroll indicators/back-to-top for navigation.
</Description>

## Animated counters

<Source
  code={`import { AnimatedCounter, AnimatedPercentage, AnimatedStat } from '@/components/ui/AnimatedCounter';

<div className="grid md:grid-cols-3 gap-6">
  <div className="text-center">
    <div className="text-3xl font-bold text-white"><AnimatedCounter value={4200} prefix="~" /></div>
    <div className="text-sm text-gray-400">Stars</div>
  </div>
  <div className="text-center">
    <div className="text-3xl font-bold text-white"><AnimatedPercentage value={98.3} /></div>
    <div className="text-sm text-gray-400">Uptime</div>
  </div>
  <AnimatedStat label="Active users" value={12345} />
</div>`}
  language="tsx"
/>

- AnimatedCounter
<ArgsTable of={AnimatedCounter} />

- AnimatedPercentage
<ArgsTable of={AnimatedPercentage} />

- AnimatedStat
<ArgsTable of={AnimatedStat} />

## Background particles

<Source
  code={`import { FloatingParticles } from '@/components/ui/FloatingParticles';

<div className="relative min-h-[200px]">
  <FloatingParticles count={30} />
  <div className="relative z-10 p-6">Foreground content</div>
</div>`}
  language="tsx"
/>

<ArgsTable of={FloatingParticles} />

## Scroll utilities

<Source
  code={`import { ScrollProgress, BackToTop } from '@/components/ui/ScrollProgress';

<div>
  <ScrollProgress />
  <div style={{ height: 800 }}>Scroll content...</div>
  <BackToTop />
</div>`}
  language="tsx"
/>

- ScrollProgress
<ArgsTable of={ScrollProgress} />

- BackToTop
<ArgsTable of={BackToTop} />

