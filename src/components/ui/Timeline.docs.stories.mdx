import { Meta, Title, Subtitle, Description, ArgsTable, Source, Canvas, Controls } from '@storybook/blocks';
import { Timeline, Accordion, Tabs } from './Timeline';
import * as TimelineStories from './Timeline.stories';
import { Calendar, Briefcase, Code2, Star, Settings } from 'lucide-react';

<Meta of={Timeline} title="UI/Timeline/Docs" tags={['docs']} />

<Title>Timeline, Accordion, Tabs</Title>

<Subtitle>Milestone timelines, content accordions, and tabbed interfaces</Subtitle>

<Description>
Use Timeline to present chronological items. Accordion reveals sections progressively. Tabs organize content into labeled panels.
</Description>

## Live examples

<Canvas of={TimelineStories.Default} />
<Source of={TimelineStories.Default} />
<Canvas of={TimelineStories.Alternating} />
<Source of={TimelineStories.Alternating} />

## Timeline

<Source
  code={`import { Timeline } from '@/components/ui/Timeline';
import { Briefcase, Code2, Star } from 'lucide-react';

const items = [
  { date: '2023', title: 'Joined PixelQuest', subtitle: 'Frontend Lead', description: 'Ownership of design system and DX', icon: Briefcase, tags: ['Leadership', 'DX'] },
  { date: '2024', title: 'Launched v2', subtitle: 'Perf & a11y', description: 'Shipped major performance and a11y upgrades', icon: Code2, tags: ['Next.js', 'Perf'] },
  { date: '2025', title: 'Awards', description: 'Recognized for delightful UX', icon: Star, tags: ['UX'] }
];

<Timeline items={items} alternating />`} language="tsx" />

<ArgsTable of={Timeline} />

## Accordion

<Source
  code={`import { Accordion } from '@/components/ui/Timeline';
import { Settings } from 'lucide-react';

const items = [
  { title: 'General', content: <div>General settings</div>, icon: Settings },
  { title: 'Advanced', content: <div>Advanced settings</div> },
];

<Accordion items={items} allowMultiple />`} language="tsx" />

<ArgsTable of={Accordion} />

## Tabs

<Source
  code={`import { Tabs } from '@/components/ui/Timeline';
import { Code2, Calendar } from 'lucide-react';

const tabs = [
  { label: 'Code', content: <div>Code panel</div>, icon: Code2 },
  { label: 'Calendar', content: <div>Calendar panel</div>, icon: Calendar },
];

<Tabs tabs={tabs} defaultTab={0} />`} language="tsx" />

<ArgsTable of={Tabs} />

