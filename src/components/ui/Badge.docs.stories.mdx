import { Meta, Title, Subtitle, Description, ArgsTable, Source, Canvas, Controls } from '@storybook/blocks';
import { Badge, BadgeGroup } from './Badge';
import * as BadgeStories from './Badge.stories';
import { Check, AlertTriangle, Info, Star, Flame } from 'lucide-react';

<Meta of={Badge} title="UI/Badge/Docs" tags={['docs']} />

<Title>Badge</Title>

<Subtitle>Status labels with variants, sizes, optional icon, and animation</Subtitle>

<Description>
Badges highlight statuses and categories. Use variants for semantic meaning and sizes for density.
Optionally include an icon and entrance/hover animation.
</Description>

## Live examples

<Canvas of={BadgeStories.Variants} />
<Source of={BadgeStories.Variants} />
<Canvas of={BadgeStories.Sizes} />
<Source of={BadgeStories.Sizes} />
<Canvas of={BadgeStories.AnimatedGroup} />
<Source of={BadgeStories.AnimatedGroup} />

## Usage

<Source
  code={`import { Badge, BadgeGroup } from '@/components/ui/Badge';
import { Check, AlertTriangle } from 'lucide-react';

export function Examples() {
  return (
    <div className="space-y-3">
      <Badge>Default</Badge>
      <Badge variant="success" icon={Check}>Success</Badge>
      <Badge variant="warning" icon={AlertTriangle}>Warning</Badge>
    </div>
  );
}`} language="tsx" />

## Variants and sizes

Variants: default, success, warning, error, info, purple

Sizes: sm, md, lg

<Source
  code={`<div className="flex flex-wrap gap-2">
  <Badge variant="default">Default</Badge>
  <Badge variant="success" icon={Check}>Deployed</Badge>
  <Badge variant="warning" icon={AlertTriangle}>Queued</Badge>
  <Badge variant="error">Failed</Badge>
  <Badge variant="info" icon={Info}>Info</Badge>
  <Badge variant="purple" icon={Star}>Featured</Badge>
</div>
<div className="flex items-center gap-2 mt-3">
  <Badge size="sm">sm</Badge>
  <Badge size="md">md</Badge>
  <Badge size="lg">lg</Badge>
</div>`}
  language="tsx"
/>

## Animated groups

<Source
  code={`<BadgeGroup>
  <Badge animate icon={Flame}>Hot</Badge>
  <Badge animate variant="success">Stable</Badge>
  <Badge animate variant="info">Docs updated</Badge>
</BadgeGroup>`}
  language="tsx"
/>

## Accessibility guidelines

- Use concise, descriptive text. Prefer text over icon-only badges.
- Ensure sufficient contrast between text and background.
- Avoid relying on color alone to convey meaning—combine color with text and/or icon.

## Props

<ArgsTable of={Badge} />

